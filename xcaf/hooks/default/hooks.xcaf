kind: hooks
version: "1.0"
events:
  PreToolUse:
    - matcher: Write|Edit
      hooks:
        - command: bash -c 'cd "${CLAUDE_PROJECT_DIR:-.}" && npx tsc -p ./ --noEmit --pretty 2>&1 | head -20; exit ${PIPESTATUS[0]}'
          type: command
